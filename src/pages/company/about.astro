---
import AboutUs from "../../components/about/AboutUs.astro";
import Hero from "../../components/about/Hero.astro";
import History from "../../components/about/History.astro";
import Office from "../../components/about/Office.astro";
import OurCommitment from "../../components/about/OurCommitment.astro";
import WhatWeDo from "../../components/about/WhatWeDo.astro";
import Layout from "../../layouts/Layout.astro";
---

<Layout
  theme='dark'
  description="Learn about Braga's mission, history, and commitment to empowering organizations with cutting-edge geospatial technology and spatial intelligence solutions."
  ><Hero /><AboutUs /><History /><WhatWeDo /><OurCommitment />
  <Office /></Layout
>

<script is:inline client:load>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("visible");
        }
      });
    },
    { threshold: 0.1 },
  );

  // Observe all animated elements
  document
    .querySelectorAll(".fade-in-up")
    .forEach((el) => observer.observe(el));
</script>

<style is:global>
  .typo-poin h1,
  .typo-poin h2,
  .typo-poin h3,
  .typo-poin h4,
  .typo-poin h5,
  .typo-poin h6 {
    font-family: "Host-Grotesk", sans-serif;
  }

  /* Initial hidden state */
  .fade-in-up {
    opacity: 0;
    transform: translateY(30px);
  }

  /* Animation when visible */
  .fade-in-up.visible {
    animation: fadeInUp 0.75s ease-out forwards;
  }

  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
