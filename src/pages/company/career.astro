---
import Hero from "../../components/career/Hero.astro";
import LifeBraga from "../../components/career/LifeBraga.astro";
import Opportunities from "../../components/career/Opportunities.astro";
import Layout from "../../layouts/Layout.astro";
---

<Layout
  theme='dark'
  isBlack={true}
  hideGridOnMobile={true}
  description="Join Braga's team of geospatial experts. Explore career opportunities in GIS technology, spatial data analysis, and location intelligence."
  ><Hero /><LifeBraga /><Opportunities /></Layout
>

<script is:inline client:load>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("visible");
        }
      });
    },
    { threshold: 0.1 },
  );

  // Observe all animated elements
  document
    .querySelectorAll(".fade-in-up")
    .forEach((el) => observer.observe(el));
</script>

<style is:global>
  .typo-poin h1,
  .typo-poin h2,
  .typo-poin h3,
  .typo-poin h4,
  .typo-poin h5,
  .typo-poin h6 {
    font-family: "Host-Grotesk", sans-serif;
  }

  /* Initial hidden state */
  .fade-in-up {
    opacity: 0;
    transform: translateY(30px);
  }

  /* Animation when visible */
  .fade-in-up.visible {
    animation: fadeInUp 0.75s ease-out forwards;
  }

  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
